{
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [{
    "url": "heroku/ruby"
  }],
  "env": {
    "GOOGLE_MAPS_API_KEY": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "project-facemask",
  "scripts": {
    "postdeploy": "rails db:schema:load db:seed",
    "test-setup": "bin/rails assets:precompile"
  }
}